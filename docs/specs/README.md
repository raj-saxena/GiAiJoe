# Spec Index

One line per spec. Read the spec itself only when you need the detail — this index exists so you don't have to glob/open all of them just to find the right one.

| Spec | Status |
|------|--------|
| [tile-tap-hello-world.md](tile-tap-hello-world.md) | Implemented — vertical-slice toolchain check (tile bounce + beep) |
